<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculation" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Calculation" parent="Base.Theme.Calculation" />

    <style name="CustomDialogStyle" parent="Theme.AppCompat.Dialog.Alert">

        <item name="android:windowBackground">@android:color/transparent</item>

        <item name="windowNoTitle">true</item>
        <item name="android:layout_width">200dp</item>

        <item name="android:background">@drawable/bac_home_list_round</item>
        <item name="android:textColor">#000000</item>
        <!-- 添加其他样式属性 -->
    </style>

</resources>